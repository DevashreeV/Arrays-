#include <stdio.h>
int main()
{
	int i;
	int x[5] = {1, 23, 5, 2, 18};
  char y[] = "DEV";
	
  printf("%d",x[0]);
	scanf("%d",&x[0]);
    printf("%c",y);
  
  for(i = 0; i < 5; i++){
     printf("\nArray element %d: %d", i, x[i]);
  }
	
	return 0;
}
